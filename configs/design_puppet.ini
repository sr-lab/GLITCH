[design]
exec_atomic_units = ["exec"]
default_variables = ["$name", "$title", "$server_facts", 
    "$trusted", "$facts", "$authenticated", "$certname", "$domain", "$extensions", "$hostname"]
var_refer_symbol = ""